/**
 * @deprecated This utility is no longer used. Modals now use portals and bringElementIntoView
 * instead of locking body scroll. This file is kept for backwards compatibility but does nothing.
 * 
 * If you need to lock scroll, use a compensated scroll lock solution instead.
 */

/**
 * @deprecated - Does nothing. Kept for backwards compatibility.
 */
export function lockBodyScroll(): void {
  // No-op: scroll locking removed
}

/**
 * @deprecated - Does nothing. Kept for backwards compatibility.
 */
export function unlockBodyScroll(): void {
  // No-op: scroll locking removed
}

/**
 * @deprecated - Does nothing. Kept for backwards compatibility.
 */
export function resetBodyScrollLock(): void {
  // No-op: scroll locking removed
}
